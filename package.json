{
  "name": "@tm-acme-shop/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for AcmeShop services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tm-acme-shop/acme-shop-shared-ts.git"
  },
  "keywords": [
    "acme-shop",
    "shared",
    "types"
  ],
  "author": "AcmeShop Team",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
